services:
  api:
    build: .

    container_name: orm_backend

    ports:
      - "4000:4000"

    env_file:
      - .env

    volumes:
      - .:/app
      - /app/node_modules

    command: npm run dev

    extra_hosts:
      - "host.docker.internal:host-gateway"